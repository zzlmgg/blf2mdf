# 领域词汇表

本文件是术语表，不是规范也不是设计记录。实现决策记 ADR，设计方案记计划文档。

## 对拍（cross-check）
用一份参照输出（CANoe 参考文件，或基准自产文件）对转换输出做等价性判定的验收手段。判定按**维度**（文件头、通道结构、逐点数值、统计组）与**对齐策略**（按组序号 / 按信号集合匹配）进行；差异为空 = 通过。自产 vs 自产用按序号对齐的精确判定（组序一致是硬性要求）；自产 vs CANoe 参考用按信号集合对齐的容差判定（参考文件组序可能与自产不同）。

## 逐位一致
对拍的最高验收标准：转换输出与参照在既定维度上无任何差异（数值精确相等或在既定容差内）。硬前提——任何架构收紧都不得削弱对拍链的判定能力。

## 回退 oracle
双实现架构：快路径（向量化解析）出错时回退到慢路径（逐容器解析），慢路径作为 oracle 提供对照；对拍测试保证两条路径输出等价（test_blf_vector fuzz 对拍 + 并行/串行等价测试）。

## 批次（batch）
一次导入中经勾选确认、共用一套平台/项目/CAN-DBC 绑定的一组待转文件；转换以批次为单位进行（批内顺序由清单给定），逐文件的结局与整体结论都挂在批次上。批次的身份 = 这一批文件 + 这一套配置；N=1 是它的退化情形。失败语义：单个文件失败不中断批次——失败挂在该文件上（结局带原因），剩余文件照常转换。

## 无残留
转换失败时的输出契约：一次失败的转换调用不留下任何输出文件——半成品被清理，输出位置保持调用前的状态（上一次成功产物保留，不做「失败即全清」）；深度落位（镜像输出树）下契约延伸到目录链，本次新建、且仍为空的目录一并回收，调用前已存在的目录不动（那可能是同批其他文件的产物落点）。取消不是失败：取消删除完整产物是调用方「放弃本次转换」的业务语义，与失败契约分离。批量下逐文件成立：某个文件的失败或取消不影响**已完成文件**的产物——那些产物不属于本次调用，不在清理范围。

## 取消信号
用户取消转换的跨模块信号：一次转换中任何检查点（扫描/解码/写 MDF）置位即取消，抛出 ConversionCancelled 由 GUI 按「已取消」处理。取消 ≠ 失败——取消删除完整产物是调用方「放弃本次转换」的业务语义（见「无残留」）。**批次取消**与单文件取消不同：停止队列 + 当前在转文件按其自身契约清理 + 已完成文件的产物全部保留——取消不等于丢弃前面已完成的工作。

## 镜像输出树
文件夹（下一阶段含压缩包）来源的产物落位：与源**同级**的 `<源名>_t/` 树——树根名加 `_t`、树内目录沿用原名、只有 MDF 文件名追加 `_t`，且只复刻通向至少一个 .blf 的目录（空目录与无关文件不复制）。它与「散 .blf 就地落位（输出与 BLF 同目录）」是按来源类型**分派**的两条并行规则：同一次导入里两种来源各按各的规则落位，互不影响。

## 统计组布局
总线统计（1s 组）的通道身份由组序携带、不写入组内：组序 = 通道序 × 统计项序。布局是跨模块输出契约——组装、写出、对拍消费三侧必须一致；统计结果对象本身不含通道身份，通道由调用方按通道序持有。

## 测量结束（global_end）
BLF 测量时段的终点 = **全文件最大对象时刻**（不只 CAN 帧：统计/标记类对象如 obj_type=11 也算，CANoe 参考统计 t 轴末点即取此值）。跨模块契约：读入侧（快路径/回退 oracle 两实现）交出「读到时间头的对象最大 ns」，converter 取全文件最大后转秒，统计聚合侧作 t 轴末点——三侧必须同值。

## 绑定（binding）
通道 ↔ DBC 文件 的对应关系：每个通道要么绑定一个 DBC（转换时按该 DBC 解码、导出该通道），要么不绑定（该通道不导出）。绑定由用户在 GUI 表格中确认；由项目映射推导的初始建议是**自动绑定建议**，其身份键为 DBC 文件路径——**显示名**（文件名 + 所在文件夹）只作展示、不参与匹配。绑定的展示状态三态：**已绑定**（绑定了一个 DBC）、**不导出**（通道在 BLF 中有信号但未绑定）、**无数据**（映射通道在 BLF 中无信号——构建期状态，用户交互后由前两态接管）。批量下「无数据」= **所有选中文件中都不存在**该通道；只在部分文件中存在的通道属正常情况——缺它的文件不导出该组，其余文件照常导出。

## 解码桶（bucket）
帧流 → 信号解码链路的按报文聚合单元：一次转换中，每个（通道 × 报文 ID）一个桶，桶内是该报文的有效帧。跨模块契约：桶身份 = 归一化键（含 EFF 位）；桶保留首帧原始 id（未知帧记账用）；桶插入顺序 = 系列输出顺序；桶只收已通过预检的帧（报文已知且帧长足够）——未知帧在入桶前已计入，解码阶段不再防御。
