# 02 — 切换到希尔塔并重画通道表

**What to build:** 测试工程师把设备从「星源」改成「希尔塔」后，已加载的 DBC 按希尔塔通道表重画，上一档的手工改动被丢掉。十个共用主名换到新通道，`VCUDebug` 绑到通道 11。切回「星源」后通道 11 回到 ZFCANT，`VCUDebug` 仍在列表里但不占通道。同一批文件共用这一档；解析、扫描或转换进行中不能改设备。

**Blocked by:** 01 — 星源保持为默认档

**Status:** ready-for-agent

- [ ] 希尔塔表为 CFCAN2→1、CFCAN3→2、PFCAN1→4、IFCAN→5、ZFCANT→6、ZFCANL→7、ZFCANF→8、CFCAN1→9、ZFCANR→10、VCUDebug→11、PFCAN2→14；通道号互不重复；不读取星源映射文件
- [ ] 已加载 DBC 时切换，按新表重算自动绑定建议并整表重画，手工改动不保留。尚未加载 DBC 时只记住当前档，通道表不凭空多行
- [ ] 换到希尔塔后，BLF 中无帧且只属于星源的通道 3、12、13、15 从表格消失；新表多出的 2、4、5、7、14 在无帧时显示「无数据」
- [ ] 通道 11 在希尔塔绑到已加载的 VCUDebug，切回星源后绑回 ZFCANT。希尔塔下若没有 VCUDebug.dbc，通道 11 不出现在自动建议里
- [ ] 换档之后的手工选择（含明确「不绑定」）在添加或移除其他 DBC、重新加载 BLF 时保留；被移除的 DBC 若使该行路径失效，则回退到当前档的自动建议
- [ ] 有帧且已绑定为「已绑定」，有帧未绑定为「不导出」。批量下「无数据」仍表示所有选中文件都没有该通道
- [ ] 超过 1 个文件的批次提示写明共用同一套设备映射、平台、项目与 CAN-DBC；只导入 1 个文件时不出现该提示
- [ ] 解析、扫描或转换进行中设备下拉框不可用。未绑定通道仍不导出原始帧，统计组规则不变
