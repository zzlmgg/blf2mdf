# 01 — 星源保持为默认档

**What to build:** 测试工程师打开工具，在「Channel ⟷ DBC」标题右侧看到设备下拉框，选项只有「星源」和「希尔塔」，打开时已选中「星源」。不切换时，选项目后的自动绑定建议与今天一致：十个 DBC 主名落到原通道。项目里的 `VCUDebug.dbc` 出现在 DBC 列表中，但不占用任何通道。对拍若不指定设备，仍按星源出绑定。

**Blocked by:** None — 可立即开工

**Status:** resolved

- [x] 下拉框只有「星源」「希尔塔」，没有空档，默认「星源」；路数标签仍在标题栏右侧
- [x] 星源下 PFCAN1→1、CFCAN2→3、CFCAN3→6、ZFCANF→8、ZFCANL→9、ZFCANR→10、ZFCANT→11、IFCAN→12、CFCAN1→13、PFCAN2→15，通道号互不重复
- [x] 星源映射文件存在时以文件为准；文件缺失或无效时回退内置星源表。内置表与文件的有效行都不含 VCUDebug
- [x] 未知档名失败，不会静默落到星源表
- [x] 选中项目后，自动绑定建议的身份仍是 DBC 文件路径；缺文件的通道不出现；主名不在星源表里的 DBC（含 VCUDebug）读入列表但不自动绑定
- [x] 对拍不改参数时仍取星源表，既有星源自动匹配与转换测试保持通过
